# AI 辅助使用与真实性规范

本项目允许 AI 辅助设计、编码、测试和文字整理，但最终责任属于六名成员。AI 不是额外成员，其输出不能替代成员理解、代码审查、实物验证和课程要求。若课程另有更严格规定，以课程规定为准。

## 允许的辅助与必须的人工作业

| 用途 | 可以辅助 | 人工必须完成 |
| --- | --- | --- |
| 架构与协议 | 比较方案、生成消息样例、检查遗漏 | 确认 UNO R4 WiFi 能力、实际传感器与接口边界，评审 MQTT/HA 契约 |
| 代码与配置 | 生成骨架、测试、注释和候选修复 | 阅读并理解关键逻辑，核对依赖/API，去除虚构接口，运行适用验证 |
| 分析与实验 | 生成分析脚本、模拟数据、实验设计 | 确定来源、指标和分母，采集真实数据，复算统计，明确限制 |
| 文档与展示 | 结构建议、语言润色、图表草稿 | 核对事实、引用、贡献与现状，区分真实、模拟和计划 |

不得让 AI 虚构传感器型号、成员姓名、已部署组件、硬件连接、实验数值、测试通过结果或参考文献。不能把 AI 生成的图表或日志冒充现场观测。任何示例结果都必须标注为示例，不填写到实测结果栏。

## 记录粒度

**不记录每次聊天。** 对影响架构、关键逻辑、实验结论或较大文档内容的实质性使用做一条简要记录即可，可放在相关 PR 或本页“实际记录”区。连续围绕同一任务的多轮交流合并记录；不用保存完整聊天历史或逐条提示词。

工具与模型版本只记录实际知道的信息；无法确认时写“版本未记录/未知”，不猜测。被完全丢弃且未影响交付的建议通常不必记录，课程另有要求除外。

## 简要记录模板

```text
记录 ID / 日期 / 负责人代号 / 关联 issue 或 PR：
工具：实际使用的工具名称；已知模型/版本，未知则注明
目的：要解决的问题与预期用途
生成：采用了哪些代码、测试、配置、图表或文字；涉及的文件/片段
人工修改：成员做了哪些核对、重写、删减与设计判断；未修改也应如实写明
验证：实际执行的命令/人工复核方法、结果与证据；未运行及其原因
限制与后续：仍待实物验证、兼容性核查或隐私审查的内容
```

核心字段是 **工具 / 目的 / 生成 / 人工修改 / 验证**，不能只写“使用 AI 提高效率”。记录中的“人工修改”必须由实际负责成员确认，AI 不能替成员声称已检查、已理解或已验收。

## 验证底线

- 代码按 [开发流程](development.md) 执行适用验证；测试命令存在不等于已运行，测试通过也不代表真实系统已部署。
- 实验依照 [实验方案](experiments.md) 记录数据来源、计时条件和丢包分母；模拟与人工构造数据不包装成实测。
- Hermes 的报告本身也是待核查输出：注明时间范围、缺测和不确定性，结论应可追溯到输入。
- AI 辅助不能改变系统安全边界：分析只读 MQTT 遥测镜像，Agent/交互的任何控制只经 HA；未知设备与不支持的动作应拒绝，不补造执行成功信息。
- 外部建议中的安装命令、依赖、许可证、API 地址和权限申请需人工核对，不能无审查执行或扩大访问权限。

## 数据与密钥

不向外部 AI 上传住户身份、寝室号、作息信息、未经同意的原始遥测、WiFi 口令、broker 凭据、HA token 或其他秘密。用去敏的最小样例或明确标注的模拟数据说明问题；也要检查日志、截图和提示词中是否夹带敏感内容。

## 实际记录

本节暂不替团队填写使用历史、人工复核或验证结论。成员采用本次或后续 AI 辅助产物后，应依据实际工具和审查结果按模板补记；此空白不代表项目未使用 AI。

### AI-2026-09-21-PRESENTATION

- **工具**：DeepSeek Harness（gpt-5.6-sol）；`guizang-ppt-skill`（来源 `op7418/guizang-ppt-skill`）。
- **目的**：依据仓库已有架构、路线图与真实性约束，生成约 10 分钟的项目规划介绍网页 PPT。
- **生成**：`presentation/dormsense-intro/index.html`、演讲者备注、制作大纲、本地 Motion One 兜底资源，以及 12 页 16:9 的 `XiaoXi-DormSense-项目介绍.pptx` 和可复现导出脚本；同时整理本地 Skill、npm/PPTX 工具缓存和预览文件的忽略规则。
- **人工修改**：待课程组成员核对项目叙事、硬件实际情况、隐私边界、引用与现场讲述节奏后填写；AI 不声称已完成人工复核。
- **验证**：已运行 Skill 自带的 presenter、Swiss deck 和 runtime-sync 静态校验；浏览器逐页视觉与交互检查结果以实际执行记录为准，不能由静态校验替代。
- **限制与后续**：PPT 只陈述规划与仓库可追溯现状，不代表真实传感器、MQTT、HA、Hermes 或家居控制已经完成部署和验收。
